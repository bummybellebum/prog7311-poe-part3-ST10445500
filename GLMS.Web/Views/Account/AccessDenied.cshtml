@{
    ViewData["Title"] = "Access Denied";
}

<section class="data-panel">
    <div class="p-5 text-center">
        <div class="metric-icon warn mx-auto">403</div>
        <h1 class="h3 mb-2">Access Denied</h1>
        <p class="text-muted mb-4">Your account role does not allow this action.</p>
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Back to Dashboard</a>
    </div>
</section>
